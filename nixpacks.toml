[phases.setup]
nixPkgs = ["python311", "gcc", "libxml2", "libxslt"]

[phases.install]
cmds = ["python -m pip install -r requirements-api.txt"]

[start]
cmd = "uvicorn api.v1.api:app --host 0.0.0.0 --port $PORT"
